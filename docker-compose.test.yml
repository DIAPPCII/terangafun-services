version: '3.7'

services:
  api:
    container_name: terangafun-api
    build:
      context: .
      dockerfile: ./Dockerfile
    command: npm run start:prod:all
    networks:
      - terangafun-network
networks:
  terangafun-network:
    name : terangafun-network